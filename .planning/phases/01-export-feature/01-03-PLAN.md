---
phase: 01-export-feature
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN
  - 01-02-PLAN
files_modified:
  - tests/api/export.test.ts
  - tests/components/export-button.test.ts
autonomous: true
requirements:
  - EXP-05  # Tests for export functionality
user_setup: []
must_haves:
  truths:
    - CSV export returns correctly structured CSV with semicolons, BOM, and Dutch headers
    - CSV export includes metadata section and per-portfolio rows with IST/SOLL diff (per D-04, D-05)
    - PDF export returns application/pdf Content-Type
    - Export API handles errors: invalid format yields 400, missing ID yields 404
    - Export API handles non-existent change requests gracefully (null from getChangeRequest yields 404)
    - ExportButton renders both CSV and PDF options
  artifacts:
    - tests/api/export.test.ts - comprehensive tests for the export API route
    - tests/components/export-button.test.ts - tests for the split button component
  key_links:
    - Tests mock getChangeRequest via vi.mock on the db module
    - Tests verify Content-Type, Content-Disposition, and response body format
    - Tests validate server-side generation via /api/export/{id}?format=csv|pdf (per D-11)
    - Tests verify the API route approach returns correct headers for direct downloads (per D-12)
    - Tests use the same pattern as existing API tests (tests/api/commits.test.ts)
---

<objective>
Create comprehensive tests for the export API route and split button component.

Purpose: Ensure export functionality works correctly for both CSV and PDF formats, handles errors gracefully, and the UI component renders properly.

Output:
- tests/api/export.test.ts - API integration tests covering CSV, PDF, and error paths
- tests/components/export-button.test.ts - component tests for the split button
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-export-feature/01-CONTEXT.md

@tests/api/commits.test.ts
@tests/api/coolify-status.test.ts
@tests/db.test.ts

@app/api/export/[id]/route.ts
@lib/export.ts
@lib/export-pdf.ts
@lib/db.ts
@lib/types.ts
@lib/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create export API tests (tests/api/export.test.ts)</name>
  <files>tests/api/export.test.ts</files>
  <action>
    Create tests/api/export.test.ts following the established test patterns in tests/api/commits.test.ts and tests/api/coolify-status.test.ts.

    The test file must have three describe blocks covering CSV format, PDF format, and error handling.

    **Setup:**

    Import from vitest: describe, it, expect, vi, beforeEach, afterEach.
    Import ChangeRequest type from @/lib/types.
    Import fixture benchmarks from @/lib/fixtures.

    Mock the db module at the top of the file:
    ```
    vi.mock("@/lib/db", () => ({ getChangeRequest: vi.fn() }));
    ```
    Import the mocked getChangeRequest and wrap with vi.mocked().

    Define a MOCK_REQUEST constant of type ChangeRequest with realistic test data mimicking a benchmark switch:
    - reference: BCM-2026-001
    - clientName: Pensioenfonds Horizon
    - clientReference: PF-HOR-001
    - requestedBy: Jan Jansen
    - rationale: Wijziging van benchmark voor betere spreiding
    - effectiveDate: 2026-09-01
    - changeType: benchmark_switch
    - status: submitted
    - createdAt: ISO date string
    - Two items using fixture benchmark data: a portfolio switching from MSCI-WORLD-NR to MSCI-ACWI-NR, and another from BLOOMBERG-EU-AGG to ICE-BOFA-EU-CORP

    In each test beforeEach:
    - vi.clearAllMocks()
    - vi.resetModules() so that dynamic imports get fresh module instances

    **describe("GET /api/export/[id] — CSV format")**:

    beforeEach: mockGetChangeRequest.mockResolvedValue(MOCK_REQUEST)

    1. should return CSV with correct Content-Type and Content-Disposition
       - Dynamically import { GET } from @/app/api/export/[id]/route
       - Call GET with new Request("http://localhost/api/export/test-id?format=csv")
       - Assert response.status is 200
       - Assert Content-Type header includes text/csv
       - Assert Content-Disposition header includes attachment and the reference BCM-2026-001

    2. should return CSV with BOM prefix and semicolon delimiters
       - Same import and call pattern
       - Assert response text starts with \uFEFF
       - Assert response text includes semicolons between data fields
       - Assert Dutch column headers are present: Portefeuille, IST Benchmark Code, SOLL Benchmark Code, Kosten

    3. should include request metadata as header rows
       - Assert CSV text includes the reference BCM-2026-001
       - Assert CSV text includes the client name
       - Assert CSV text includes the requester Jan Jansen
       - Assert CSV text includes the rationale text

    4. should include per-portfolio IST/SOLL diff data
       - Assert CSV text includes portfolio names from mock data
       - Assert CSV text includes previous benchmark codes (MSCI-WORLD-NR, BLOOMBERG-EU-AGG)
       - Assert CSV text includes requested benchmark codes (MSCI-ACWI-NR, ICE-BOFA-EU-CORP)

    **describe("GET /api/export/[id] — PDF format")**:

    beforeEach: mockGetChangeRequest.mockResolvedValue(MOCK_REQUEST)

    5. should return PDF with correct Content-Type
       - Call GET with ?format=pdf
       - Assert response.status is 200
       - Assert Content-Type is application/pdf
       - Assert Content-Disposition includes attachment

    6. should include reference in PDF Content-Disposition filename
       - Assert Content-Disposition filename contains BCM-2026-001

    **describe("GET /api/export/[id] — error handling")**:

    7. should return 400 for invalid format
       - mockGetChangeRequest.mockResolvedValue(MOCK_REQUEST)
       - Call GET with ?format=docx
       - Assert response.status is 400
       - Assert response body contains Ongeldig exportformaat

    8. should return 404 for non-existent change request
       - mockGetChangeRequest.mockResolvedValue(null)
       - Call GET with ?format=csv
       - Assert response.status is 404
       - Assert response body contains niet gevonden

    9. should return 500 when database throws
       - mockGetChangeRequest.mockRejectedValue(new Error("DB connection failed"))
       - Call GET with ?format=csv
       - Assert response.status is 500
       - Assert response body contains Export mislukt

    The route handler should be written to parse format from new URL(request.url).searchParams (works in both test and production), rather than relying on NextRequest-specific APIs.

    The verify block must pass: npx vitest run tests/api/export.test.ts exits 0.
  </action>
  <verify>
    <automated>npx vitest run tests/api/export.test.ts 2>&1</automated>
  </verify>
  <done>
    - tests/api/export.test.ts exists with 9+ tests covering CSV, PDF, and error scenarios
    - All tests pass with mocked getChangeRequest
    - Tests follow existing vitest patterns (vi.mock, dynamic import, vi.resetModules)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create export button component tests (tests/components/export-button.test.ts)</name>
  <files>tests/components/export-button.test.ts</files>
  <action>
    Create tests/components/export-button.test.ts with basic rendering and interaction tests.

    Since the test environment is node (not jsdom), test the logic and structure of the ExportButton component without requiring browser DOM APIs. Two approaches:

    **Approach A (recommended):** Test the component's module exports and behavior via source inspection and by testing any extracted pure utility functions within the component.

    **describe("ExportButton — component structure")**:

    1. should export the ExportButton component
       - Dynamically import @/components/export-button
       - Assert that ExportButton is defined and is a function

    2. should accept changeRequestId prop
       - The component file should export a type or interface for its props
       - Assert that the exported type includes changeRequestId: string

    3. should reference CSV and PDF download URLs
       - Read the source file content using fs
       - Assert that the file contains /api/export/ string (the API URL pattern)
       - Assert that the file contains format=csv and format=pdf
       - Assert that the file contains Exporteren for the loading text

    4. should have split button structure
       - Assert source contains export-split class references
       - Assert source contains both main button and arrow button hover text

    **describe("ExportButton — behavior")**:

    These tests verify the component's state management:

    5. should export EXPORT_FORMATS constant (if extracted) or reference both format options
       - Assert source contains csv and pdf format identifiers

    6. should have click-outside handler for dropdown
       - Assert source contains mousedown or click event listener on document

    Write synchronous importable tests — no jsdom or browser environment needed. Focus on module structure, exports, and source-level behavioral assertions.

    Follow the pattern from tests/components/updates-timeline.test.ts for vitest structure.
  </action>
  <verify>
    <automated>npx vitest run tests/components/export-button.test.ts 2>&1</automated>
  </verify>
  <done>
    - tests/components/export-button.test.ts exists
    - Tests verify component exports, props interface, API URL references, and behavioral structure
    - All tests pass in vitest node environment
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Imports | Tests dynamically import production code; mock boundaries must be clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Spoofing | Export API tests | low | accept | Tests use mocked getChangeRequest; no real DB needed |
| T-01-09 | Tampering | ExportButton source tests | low | accept | Source-inspection tests assert API URL patterns but don't execute the component in a DOM |
</threat_model>

<verification>
- npx vitest run tests/api/export.test.ts passes all 9+ tests
- npx vitest run tests/components/export-button.test.ts passes all 6+ tests
- npx vitest run passes the full test suite (existing tests unaffected)
</verification>

<success_criteria>
- All export API route behaviors covered by passing tests
- Export button component structure and behaviors covered by passing tests
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/01-export-feature/01-03-SUMMARY.md` when done
</output>
