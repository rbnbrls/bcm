---
phase: 02-e2e-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-playwright-setup
files_modified:
  - tests/e2e/benchmark-switch.spec.ts
  - tests/e2e/benchmark-catalog.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - User can run the benchmark switch E2E test and it navigates through the full flow: homepage → select client → select portfolio → set SOLL benchmark → fill form → submit
    - User can run the benchmark catalog E2E test and it verifies the catalog page renders benchmarks correctly
    - Both tests pass against the Next.js dev server with demo fixture data (no database required)
  artifacts:
    - tests/e2e/benchmark-switch.spec.ts — full flow test for benchmark switch
    - tests/e2e/benchmark-catalog.spec.ts — catalog browsing and data verification tests
  key_links:
    - benchmark-switch.spec.ts → helpers.ts navigation functions — tests depend on helpers for page navigation
    - benchmark-switch.spec.ts → DOM structure of BenchmarkChangeForm — tests use selectors matching the form structure
    - benchmark-catalog.spec.ts → DOM structure of BenchmarkCatalogTable — tests verify specific benchmark data from fixtures
---

<objective>
Create E2E tests for the benchmark switch flow and benchmark catalog browsing.

Purpose: Verify the two most critical user journeys work end-to-end — submitting a benchmark change request, and browsing the benchmark catalog.
Output: Two test spec files covering these flows.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/project.md
@.planning/roadmap.md
@components/benchmark-change-form.tsx
@components/benchmark-new-form.tsx
@app/benchmarks/benchmark-catalog-table.tsx
@app/changes/[id]/page.tsx
@lib/fixtures.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for benchmark switch flow</name>
  <files>tests/e2e/benchmark-switch.spec.ts</files>
  <action>
    Create `tests/e2e/benchmark-switch.spec.ts` with the following test structure using `@playwright/test`:

    Import helpers from `./helpers`:
    - `navigateToBenchmarkSwitch`, `selectClient`, `selectPortfolio`, `setSOLLBenchmark`, `fillFormFields`, `submitForm`
    - Fixture IDs: `VALID_BENCHMARK_1_ID`, `VALID_BENCHMARK_2_ID`, `VALID_CLIENT_ID`
    - `DEMO_CLIENT_NAME`, `DEMO_PORTFOLIO_NAME`
    - Import `{ test, expect }` from `@playwright/test`.

    Use `test.describe('Benchmark switch flow')` with these test cases:

    1. `test('full benchmark switch flow: homepage to submission')` — the happy path:
       - Call `await navigateToBenchmarkSwitch(page)`.
       - Verify URL contains `/changes/new`.
       - Verify the page heading "Benchmarkwissel" is visible.
       - The default client is pre-selected ("Pensioenfonds Horizon"). Verify the select has value matching `VALID_CLIENT_ID`.
       - Check the portfolio checkbox for `DEMO_PORTFOLIO_NAME` ("Rendementsportefeuille").
       - The SOLL dropdown for that portfolio becomes enabled. Select `VALID_BENCHMARK_2_ID` (MSCI-ACWI-NR) as the target benchmark.
       - Fill form fields:
         - `requestedBy`: "E2E Test User"
         - `rationale`: "E2E test benchmark switch — automated verification."
         - `effectiveDate`: Set to a date 30 days from now (use JavaScript date math to avoid past-date validation).
       - Click the submit button: `await submitForm(page)`.
       - After submission, verify the page navigates to `/changes/[id]` (URL matches pattern `/changes/` followed by a UUID).
       - Verify the change request detail page shows the reference (starts with "BCM-") and status is "submitted" or similar.

    2. `test('selects different client and portfolio, verifies IST display')`:
       - Navigate to `/changes/new`.
       - Select "Stichting Pensioen Zeker" from the client dropdown.
       - Verify portfolio "Return portefeuille" appears.
       - Check the portfolio checkbox.
       - Verify the IST benchmark label shows "MSCI-ACWI-NR" (which is the currentBenchmark for that portfolio per fixtures).
       - Select a SOLL benchmark different from IST.

    3. `test('disables SOLL dropdown when no portfolio selected')`:
       - Navigate to `/changes/new`.
       - Verify the SOLL select inside any portfolio card has the `disabled` attribute (no portfolio is selected yet — per the component code, `disabled={!selected}`).
       - Select a portfolio.
       - Verify the SOLL select becomes enabled.
       - Deselect the portfolio.
       - Verify the SOLL select becomes disabled again.

    4. `test('shows validation errors on submit with empty form')`:
       - Navigate to `/changes/new`.
       - Without selecting any portfolio, try clicking the submit button.
       - The submit button should be disabled (button has `disabled` attribute and `totalSwitchPortfolios === 0`).
       - Verify the button text says "Genereer change request →" or is disabled.

    Use `test.use({ storageState: undefined })` to ensure clean state. No need for auth — app works with demo data.

    Use `await page.waitForLoadState('networkidle')` after navigation where appropriate.
  </action>
  <verify>
    <automated>node -e "require('fs').statSync('tests/e2e/benchmark-switch.spec.ts'); console.log('spec file exists')"</automated>
  </verify>
  <done>
    - `tests/e2e/benchmark-switch.spec.ts` has 4 test cases covering happy path, client selection, disabled state, and validation
    - Tests reference correct DOM selectors from the BenchmarkChangeForm component
    - Tests use demo fixture data (no database dependency)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create E2E test for benchmark catalog browsing</name>
  <files>tests/e2e/benchmark-catalog.spec.ts</files>
  <action>
    Create `tests/e2e/benchmark-catalog.spec.ts` with the following test structure:

    Import helpers from `./helpers`:
    - `navigateToCatalog`
    - Import `{ test, expect }` from `@playwright/test`.
    - Import fixture constants from helpers.

    Use `test.describe('Benchmark catalog browsing')` with these test cases:

    1. `test('shows all benchmarks in catalog table')`:
       - Navigate to `/benchmarks` (using `navigateToCatalog` from helpers or direct `page.goto('/benchmarks')`).
       - Verify the page heading "Benchmark catalogus" is visible.
       - Verify multiple benchmark rows are visible in the catalog table.
       - The table should have header columns: "Short name" (or "Code"), "Long name" (or "Name"), "Asset class" (or "Asset class"), "Kosten" (or "Cost"), "Leverancier" (or "Provider").
       - Verify specific demo benchmarks are listed:
         - "MSCI-WORLD-NR" is visible
         - "MSCI-ACWI-NR" is visible
         - "Bloomberg Euro Aggregate" is visible

    2. `test('displays cost summary cards')`:
       - Navigate to `/benchmarks`.
       - Verify three cost cards are displayed: "Benchmarkwissel", "Benchmarkwissel + nieuw", and "Nieuwe benchmark".
       - Verify each card shows doorlooptijd and kosten information.
       - Verify "Nieuwe benchmark" card shows "€ 5.000".

    3. `test('shows benchmarks filtered by asset class (visual grouping)')`:
       - Navigate to `/benchmarks`.
       - Note the table groups benchmarks by asset class (the component likely groups them). If the component renders sections per asset class, verify that "Aandelen" section contains MSCI benchmarks and "Obligaties" section contains Bloomberg/ICE benchmarks.
       - If no visual grouping exists, at minimum verify that different asset class values appear in the table (e.g., "Aandelen", "Obligaties", "Alternatieven", "Vastgoed").

    4. `test('navigates to catalog from homepage')`:
       - Use `navigateToCatalog(page)` from helpers.
       - Verify URL contains `/benchmarks`.
       - Verify the page heading "Benchmark catalogus" is visible.
       - This tests the homepage CTA link works correctly.

    All tests should use demo fixture data — no database needed. The `getBenchmarks()` function returns fixture data when no `DATABASE_URL` is set.
  </action>
  <verify>
    <automated>node -e "require('fs').statSync('tests/e2e/benchmark-catalog.spec.ts'); console.log('spec file exists')"</automated>
  </verify>
  <done>
    - `tests/e2e/benchmark-catalog.spec.ts` has 4 test cases covering catalog display, cost cards, asset class grouping, and homepage navigation
    - Tests verify fixture benchmark data is rendered correctly
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | E2E test code — no new trust boundaries introduced. Tests run against dev server using demo fixture data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | test spec files | low | accept | Tests run locally/in-CI with no production access; demo data only |
</threat_model>

<verification>
- `npx playwright test tests/e2e/benchmark-switch.spec.ts` passes (requires dev server running, or use webServer auto-start)
- `npx playwright test tests/e2e/benchmark-catalog.spec.ts` passes
- All tests pass with demo fixture data (no DATABASE_URL required)
</verification>

<success_criteria>
- Benchmark switch flow is tested end-to-end: homepage → form → client select → portfolio select → SOLL set → submit → redirect
- Benchmark catalog page is verified: table rendering, cost cards, fixture data presence
- Tests are idempotent (no shared state)
</success_criteria>

<output>
Create `.planning/phases/02-e2e-testing/02-02-SUMMARY.md` when done
</output>
