---
phase: 03-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/not-found.tsx
  - app/changes/[id]/page.tsx
  - app/updates/page.tsx
  - components/feedback-button.tsx
  - components/updates-timeline.tsx
  - components/benchmark-change-form.tsx
  - components/benchmark-new-form.tsx
  - components/export-button.tsx
  - app/benchmarks/benchmark-catalog-table.tsx
  - app/admin/client-config/client-config-table.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Keyboard users see visible focus rings on every interactive element when tabbing
    - Screen readers get ARIA labels, roles, and live regions for all dynamic content
    - Skeleton CSS classes exist and are ready for loading.tsx files
    - Focus-visible outlines never clip on buttons or interactive elements
  artifacts:
    - app/globals.css — contains :focus-visible rules, skeleton CSS primitives, accent contrast remediation
    - All component files — have aria-* attributes per UI-SPEC ARIA Additions table
  key_links:
    - Interactive elements → :focus-visible outlines
    - Skeleton CSS classes → loading.tsx files in Plan 02
    - Form fields → aria-describedby linkage
---

<objective>
Add visible focus-visible outlines, skeleton CSS primitives, and ARIA attributes to all interactive elements.

Purpose: Meet WCAG 2.1 AA keyboard navigation (2.1.1) and focus visible (2.4.7) criteria. Establish skeleton CSS foundation for Plan 02 loading states. Provide assistive technology context across all surfaces.

Output: Globals.css with :focus-visible + skeleton classes + accent contrast fixes. All components annotated with ARIA attributes per UI-SPEC.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-polish/03-CONTEXT.md
@.planning/phases/03-ui-polish/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS primitives + layout/static page ARIA</name>
  <files>
    app/globals.css
    app/layout.tsx
    app/page.tsx
    app/not-found.tsx
    app/changes/[id]/page.tsx
    app/updates/page.tsx
  </files>
  <action>
    Per UI-SPEC Accessibility Improvements and Execution Checklist:

    **globals.css — Add at the end, before the responsive block:**

    1. `:focus-visible` global outline rule (`outline: 2px solid var(--accent); outline-offset: 2px; border-radius: 2px`). Do NOT remove existing `outline: none` rules — this plan only adds the `:focus-visible` counterpart alongside existing rules. If any element currently has `outline: none` on `:focus` but no `:focus-visible` variant exists, add one using the accent variable.

    2. Button-specific `:focus-visible` overrides (`.button:focus-visible`, `.button-primary:focus-visible`, `.button-secondary:focus-visible`, `.button-ghost:focus-visible`) using `--accent-deep` for higher contrast.

    3. Skeleton CSS primitives: `@keyframes shimmer`, `.skeleton`, `.skeleton-text`, `.skeleton-heading`, `.skeleton-card` classes with shimmer animation (linear-gradient with `--panel` base). Use exactly the CSS from UI-SPEC § Loading States → Skeleton CSS Primitives.

    4. Accent contrast remediation per UI-SPEC Color section: Change `color: var(--accent)` to `color: var(--accent-deep)` on `.button-ghost`, `.feedback-success a`, `.catalog-section a`, and any other body-size (≤18px) `--accent` text-on-white locations found in globals.css. Keep `--accent` for large/display text and backgrounds.

    **app/layout.tsx — ARIA additions:**
    - The existing `nav` already has `aria-label="Hoofdnavigatie"` ✅ — keep.
    - Change `.user-chip` div to add appropriate role. The div shows "RV Vermogensbeheerder" — it's read-only display info. Add `role="status"` and `aria-live="polite"` so screen readers announce it.
    - Add `aria-label="Updates en changelog"` to the updates link (already has `aria-label="Updates / Changelog"` — enhance to `aria-label="Updates en changelog"` for consistency).
    - The topbar `<header>` element — add `role="banner"` (implicit on `<header>` in modern HTML but explicit for older AT — keep as is, `<header>` is fine).

    **app/page.tsx — ARIA improvements:**
    - The `.hero` section has no `aria-label`. Add `role="region"` with `aria-label="Introductie"`.
    - The `.workflow-card` sections — add descriptive `aria-label` attrs (e.g., `aria-label="Benchmarkwissel use case"`, `aria-label="Nieuwe benchmark use case"`).
    - `.status-grid` already has `aria-label="Overzicht"` ✅. Keep it.
    - Stat cards are `<article>` elements — add `aria-label` per stat type (e.g., `aria-label="Actieve klanten"`).

    **app/not-found.tsx — ARIA:**
    - Add `role="alert"` to the empty-state wrapper since this is an error display.

    **app/changes/[id]/page.tsx — ARIA:**
    - The `.status-pill` span (`{request.status === "submitted" ? "Ingediend" : request.status}`) — add `role="status"` and `aria-live="polite"` so screen readers announce status changes.
    - Add `aria-label` to each `section` element that lacks a heading sibling for context.
    - The `.diff-section` IST/SOLL labels — ensure `dt`/`dd` or appropriate semantic markers. Current code uses `<div>` with class `diff-line`. Add `role="listitem"` within a `role="list"` wrapper.

    **app/updates/page.tsx — ARIA:**
    - The main content area — add `role="region"` with `aria-label="Recente wijzigingen"`.
    - Keep existing `<section>` landmark semantics.
  </action>
  <verify>
    <automated>grep -c ':focus-visible' app/globals.css | grep -qv '0' && grep -c 'skeleton' app/globals.css | grep -qv '0' && grep -c 'aria-label' app/layout.tsx | grep -qv '0'</automated>
  </verify>
  <done>
    Focus-visible outlines added to globals.css for all interactive elements. Skeleton CSS classes available. ARIA attributes present on layout, static pages, change detail, and updates page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Form component ARIA + ExportButton error upgrade</name>
  <files>
    components/feedback-button.tsx
    components/export-button.tsx
    components/benchmark-change-form.tsx
    components/benchmark-new-form.tsx
  </files>
  <action>
    Per UI-SPEC ARIA Additions table, Priority Matrix, and Execution Checklist:

    **components/feedback-button.tsx:**
    - Add `aria-modal="true"` to the `.feedback-modal` div (already has `role="dialog"`).
    - Verify trigger button has `aria-haspopup="dialog"` — add if missing.
    - Verify close button `aria-label="Sluiten"` is present ✅.
    - Add `aria-label` to the inline SVG icons.

    **components/export-button.tsx:**
    - Add `aria-expanded` to the dropdown arrow button (tied to `open` state).
    - Add `role="menu"` to `.export-split__dropdown` div with `aria-orientation="vertical"`.
    - Add `role="menuitem"` to each dropdown button.
    - Add `aria-haspopup="menu"` to the arrow trigger button.
    - **Upgrade error handling per UI-SPEC § Client-Side Error Upgrades**: Replace the `alert()` call in `catch` block with rendering an inline `.form-errors` div. Add an `error` state (useState<string|null>) and render it below the split button when non-null. Clear it on next successful download attempt.

    **components/benchmark-change-form.tsx:**
    - The `<select>` for client — add `aria-describedby` referencing a hint element if one exists. Currently the heading `<p>` serves as description. No change needed if the semantic `<label>` + `<span>` inside provides sufficient context.
    - The hidden `<input>` fields (`clientId`, `items`, `newBenchmarkItems`) — no ARIA needed (hidden).
    - Portfolio checkboxes — each `<input type="checkbox">` inside `.portfolio-toggle` already has a visible `<span>` label. Add `aria-label` matching the portfolio name for extra clarity: `aria-label="Selecteer ${portfolio.name}"`.
    - Benchmark `<select>` dropdowns inside each portfolio card — add `aria-label="Kies SOLL benchmark voor ${portfolio.name}"`.
    - The "Reden van de wijziging" textarea — verify `aria-describedby` links to the placeholder hint. The `<label>` wrapping with `<span>` inside provides sufficient context in this design.
    - `.form-errors` div (role="alert") — already has `role="alert"` ✅. Add `aria-live="polite"` as well.
    - Add `aria-label` to the step section numbers (`.section-number` divs) for screen reader context.
    - Add `aria-invalid` to fields when `state.issues` includes field-level errors. For simplicity, mark the form-level validation (state.issues exists) by adding `aria-invalid="true"` to the submit button area. (Native browser validation already handles field-level `:invalid` styling.)

    **components/benchmark-new-form.tsx:**
    - Client `<select>` — add `aria-label="Selecteer klant"`.
    - Short name `<input>` — add `aria-label="Short name (code)"` (already has `<span>Short name (code)</span>` inside label, so this is redundant but explicit).
    - Currency `<input>` — add `aria-label="Valuta"`.
    - Long name `<input>` — add `aria-label="Long name"`.
    - Asset class `<select>` — add `aria-label="Asset class"`.
    - Reason textarea — add `aria-label="Reden van de aanvraag"`.
    - Effective date `<input>` — add `aria-label="Gewenste ingangsdatum"`.
    - Requested by `<input>` — add `aria-label="Aanvrager"`.
    - `.section-number` divs — add `aria-hidden="true"` since these are visual step indicators already described by the heading.
    - `.form-errors` div — already has `role="alert"` ✅. Add `aria-live="polite"`.
  </action>
  <verify>
    <automated>grep -c 'aria-expanded\|role="menu"\|aria-modal\|aria-label' components/export-button.tsx | grep -qv '0' && grep -c 'aria-label' components/benchmark-change-form.tsx | grep -qv '0' && grep -c 'alert' components/export-button.tsx | grep -qv '0'</automated>
  </verify>
  <done>
    All form components have ARIA attributes per spec. ExportButton uses inline error display instead of alert(). FeedbackButton has aria-modal="true".
  </done>
</task>

<task type="auto">
  <name>Task 3: Table/display component ARIA + table error handling upgrade</name>
  <files>
    app/benchmarks/benchmark-catalog-table.tsx
    app/admin/client-config/client-config-table.tsx
    components/updates-timeline.tsx
  </files>
  <action>
    Per UI-SPEC ARIA Additions table, Priority Matrix, and Execution Checklist:

    **app/benchmarks/benchmark-catalog-table.tsx:**
    - Search `.catalog-search` input — add `aria-label="Zoeken in benchmark catalogus"`.
    - Sort header buttons — add `aria-sort` attribute that reflects current sort state:
      - When `sortKey === col.key && sortDir === "asc"`: `aria-sort="ascending"`
      - When `sortKey === col.key && sortDir === "desc"`: `aria-sort="descending"`
      - When not active: remove the attribute or set `aria-sort="none"`.
    - The `<table>` element — add `<caption>` with text "Benchmark catalogus met zoek- en sorteerfuncties".
    - **Error handling upgrade per UI-SPEC § Client-Side Error Upgrades**: Currently this table receives pre-fetched benchmarks as props and has no error state. Since data is fetched server-side, error handling is via error.tsx (Plan 02). No client-side error upgrade needed here — the table is a pure display component of server-fetched data.

    **app/admin/client-config/client-config-table.tsx:**
    - Filter toggle button `.config-filter-toggle` — add `aria-expanded` tied to `showFilters` state.
    - Sort header buttons — add `aria-sort` matching current sort state (same pattern as benchmark table).
    - The `<table>` element — add `<caption>` with text "Client configuratie met filter- en sorteerfuncties".
    - Column filter `<input>` elements — add `aria-label="Filter op ${col.label.toLowerCase()}"` for each.
    - **Error handling**: Same as benchmark table — data is server-fetched. Error handling via error.tsx. No client-side upgrade needed.

    **components/updates-timeline.tsx:**
    - `.timeline-spinner` SVG — add `aria-label="Bezig met laden…"` for screen reader context per UI-SPEC ARIA table.
    - The `.pill-spinner` span — add `role="status"` and `aria-label="Deploying"` (renders when `status?.deploying` is true).
    - The table — add `<caption>` with text "Overzicht van recente wijzigingen aan de BCM-app".
    - Sort buttons (if any) — the updates timeline has no sorting (commits are sorted by date), so no aria-sort needed.
    - The `.empty-state` div — no ARIA changes needed; it already renders plain visible text.
    - The timeline retry button — add `aria-label="Opnieuw proberen"`.

    **Global rule applied across all files**: Do not remove or modify existing class names, DOM structure, event handlers, or business logic. Only add aria-* attributes, role attributes, and the ExportButton error upgrade which replaces `alert()` with an inline `.form-errors` div (preserve the same error message text).
  </action>
  <verify>
    <automated>grep -c 'aria-label' app/benchmarks/benchmark-catalog-table.tsx | grep -qv '0' && grep -c 'aria-expanded' app/admin/client-config/client-config-table.tsx | grep -qv '0' && grep -c 'aria-label' components/updates-timeline.tsx | grep -qv '0'</automated>
  </verify>
  <done>
    All table/display components have ARIA attributes per spec. ExportButton has inline error display. Sort headers have aria-sort. Table captions present. Filter toggle has aria-expanded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→DOM | ARIA attributes and roles are static — no user input controls attributes; low risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | aria-label on elements | low | accept | Static Dutch-language labels; no dynamic content leakage |
| T-03-SC | Tampering | npm install (Plan 03 only) | high | mitigate | Package legitimacy audit in Plan 03 before @axe-core/playwright install |
</threat_model>

<verification>
- [ ] `npm run build` succeeds (run after Plan 01, Plan 02, and Plan 03 complete — not standalone)
- [ ] Each interactive element has visible focus ring when tabbed to
- [ ] Skeleton CSS classes render with shimmer animation
- [ ] No existing component functionality broken by ARIA additions
</verification>

<success_criteria>
- All interactive elements have `:focus-visible` outlines in globals.css
- Skeleton CSS primitives available for Plan 02 loading.tsx files
- All components have ARIA attributes per UI-SPEC ARIA Additions table
- ExportButton uses inline error display instead of `alert()`
- Table components have `<caption>` elements and sort/filter ARIA
- Focus-visible outlines use `--accent`/`--accent-deep` tokens
</success_criteria>
