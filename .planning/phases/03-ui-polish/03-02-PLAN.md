---
phase: 03-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/loading.tsx
  - app/benchmarks/loading.tsx
  - app/benchmark-aanvraag/loading.tsx
  - app/changes/new/loading.tsx
  - app/changes/[id]/loading.tsx
  - app/admin/client-config/loading.tsx
  - app/error.tsx
  - app/benchmarks/error.tsx
  - app/benchmark-aanvraag/error.tsx
  - app/changes/new/error.tsx
  - app/changes/[id]/error.tsx
  - app/admin/client-config/error.tsx
  - app/updates/error.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Every async route group shows a skeleton placeholder while data loads
    - Every route group catches rendering errors without crashing the full page
    - Error pages offer "Probeer opnieuw" retry and "Naar home" navigation
    - Skeleton forms/detail/tables match the shape of their corresponding pages
  artifacts:
    - app/*/loading.tsx — one per async route group
    - app/*/error.tsx — one per route group (including root app/error.tsx)
  key_links:
    - Skeleton CSS classes (from Plan 01) → rendered in loading.tsx components
    - error.tsx reset() → Next.js error recovery mechanism
---

<objective>
Create loading.tsx skeleton files for all async route groups and error.tsx boundary files for all route groups.

Purpose: Eliminate blank-screen flash during server data fetching (D-03). Prevent full-page crashes from unhandled rendering errors (D-04). Provide graceful error recovery with retry and home navigation.

Output: Six loading.tsx files with skeleton placeholders using shimmer CSS from Plan 01. Seven error.tsx boundary files (root + 6 route groups) per UI-SPEC visual contract.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-polish/03-CONTEXT.md
@.planning/phases/03-ui-polish/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeleton files for all async route groups</name>
  <files>
    app/loading.tsx
    app/benchmarks/loading.tsx
    app/benchmark-aanvraag/loading.tsx
    app/changes/new/loading.tsx
    app/changes/[id]/loading.tsx
    app/admin/client-config/loading.tsx
  </files>
  <action>
    Per UI-SPEC § Loading States → Route-Level loading.tsx and § Skeleton CSS Primitives:

    Create each loading.tsx as a **server component** (no "use client") that renders skeleton divs using CSS classes from Plan 01 (`.skeleton`, `.skeleton-text`, `.skeleton-heading`, `.skeleton-card`). Each skeleton wraps in `<div className="page-shell">` with matching shell class.

    Import none — these are direct markup using global CSS classes. Each file's default export is an async-compatible React component.

    **app/loading.tsx** (root, catches `/`):
    ```tsx
    export default function Loading() {
      return (
        <div className="page-shell home-shell">
          <div className="hero">
            <div className="skeleton skeleton-text" style={{ width: "140px", marginBottom: 16 }} />
            <div className="skeleton skeleton-heading" style={{ width: "80%" }} />
            <div className="skeleton skeleton-text" style={{ width: "60%", marginTop: 16 }} />
          </div>
          <div className="status-grid" style={{ marginTop: 48 }}>
            <div className="skeleton skeleton-card" />
            <div className="skeleton skeleton-card" />
            <div className="skeleton skeleton-card" />
          </div>
        </div>
      );
    }
    ```
    Style `width` values as inline styles to approximate content shape. Do NOT use percentage widths that collapse. Use fixed pixel widths per skeleton class.

    **app/benchmarks/loading.tsx** — skeleton table: page-intro heading + toolbar + 5 table rows.
    - Render `.page-shell.config-shell` wrapper.
    - Page intro: `.skeleton-heading` (60% width), `.skeleton-text` (80% width).
    - Toolbar: `.skeleton` div (height 32px, width 200px, marginBottom 12).
    - Table wrapper: 6 skeleton rows, each with 6 `.skeleton-text` inline-blocks mimicking columns. First row slightly taller for "header" appearance.

    **app/benchmark-aanvraag/loading.tsx** — skeleton form: 3 section placeholders.
    - Render `.page-shell.request-shell` wrapper.
    - Page intro: `.skeleton-heading` (50% width), `.skeleton-text` (70% width).
    - Three form section cards: each is `.skeleton-card` (height 160px) with 3-4 inner `.skeleton-text` lines.

    **app/changes/new/loading.tsx** — skeleton form: 4 section placeholders.
    - Render `.page-shell.request-shell` wrapper.
    - Page intro: `.skeleton-heading` (50% width), `.skeleton-text` (70% width).
    - Four form section cards: each `.skeleton-card` (height 180px) with skeleton lines. The 4th section (submit) is shorter (height 100px) with a skeleton block for the submit button.

    **app/changes/[id]/loading.tsx** — skeleton detail: header + 2 section cards.
    - Render `.page-shell.request-shell` wrapper.
    - Request header: `.skeleton-heading` (50% width), 2x `.skeleton-text`.
    - Overview section: 4 skeleton rows in a grid (like the `.request-overview` layout).
    - Diff section or NB detail: `.skeleton-card` (height 200px) with 5 skeleton lines.

    **app/admin/client-config/loading.tsx** — skeleton table: toolbar + table.
    - Render `.page-shell.config-shell` wrapper.
    - Page intro: `.skeleton-heading` (45% width), `.skeleton-text` (75% width).
    - Toolbar: `.skeleton` div (height 32px, width 160px).
    - Table: 5 skeleton rows, each with 4 skeleton-text inline-blocks.

    **Important**: All loading.tsx files MUST use `export default function Loading()` — no `async` keyword needed (these are pure presentational components). Do NOT add "use client" directive. The `.skeleton-*` CSS classes are global and available from Plan 01's globals.css additions.

    Per UI-SPEC § Copywriting Contract: Each skeleton wrapper should have `aria-label="Bezig met laden…"` for screen reader context.
  </action>
  <verify>
    <automated>
      for f in app/loading.tsx app/benchmarks/loading.tsx app/benchmark-aanvraag/loading.tsx app/changes/new/loading.tsx app/changes/\[id\]/loading.tsx app/admin/client-config/loading.tsx; do
        if [ ! -f "$f" ]; then echo "MISSING: $f"; exit 1; fi
      done
      echo "All loading.tsx files exist"
    </automated>
  </verify>
  <done>
    Six loading.tsx files created matching their corresponding async page layouts. All use skeleton CSS classes from Plan 01. Each has aria-label="Bezig met laden…".
  </done>
</task>

<task type="auto">
  <name>Task 2: Create error.tsx boundary files for all route groups</name>
  <files>
    app/error.tsx
    app/benchmarks/error.tsx
    app/benchmark-aanvraag/error.tsx
    app/changes/new/error.tsx
    app/changes/[id]/error.tsx
    app/admin/client-config/error.tsx
    app/updates/error.tsx
  </files>
  <action>
    Per UI-SPEC § Error States → Route-Level error.tsx and § Error Page Visual Contract:

    Each error.tsx uses this pattern (adapt per route):
    ```tsx
    "use client";

    import Link from "next/link";

    export default function Error({
      error,
      reset,
    }: {
      error: Error & { digest?: string };
      reset: () => void;
    }) {
      console.error(error);

      return (
        <div className="page-shell empty-state">
          <p className="eyebrow">FOUT</p>
          <h1>{TITLE}</h1>
          <p>{DESCRIPTION}</p>
          <div style={{ display: "flex", gap: 12, marginTop: 24 }}>
            <button className="button button-primary" onClick={reset} type="button">
              Probeer opnieuw
            </button>
            <Link className="button button-secondary" href="/">
              Naar home
            </Link>
          </div>
        </div>
      );
    }
    ```

    Per-route customizations:

    | File | TITLE | DESCRIPTION |
    |------|-------|-------------|
    | `app/error.tsx` (root/global) | "Er is een fout opgetreden" | "Probeer het nog een keer of ga terug naar de homepagina." |
    | `app/benchmarks/error.tsx` | "Catalogus laden mislukt" | "Controleer je verbinding en probeer het opnieuw." |
    | `app/benchmark-aanvraag/error.tsx` | "Formulier laden mislukt" | "Controleer je verbinding en probeer het opnieuw." |
    | `app/changes/new/error.tsx` | "Aanvraagformulier laden mislukt" | "Controleer je verbinding en probeer het opnieuw." |
    | `app/changes/[id]/error.tsx` | "Change request laden mislukt" | "Controleer de link of ga terug naar het overzicht." |
    | `app/admin/client-config/error.tsx` | "Configuratie laden mislukt" | "Controleer je verbinding en probeer het opnieuw." |
    | `app/updates/error.tsx` | "Wijzigingen laden mislukt" | "Controleer je verbinding en probeer het opnieuw." |

    **app/changes/[id]/error.tsx** additionally: Replace "Probeer opnieuw" with a link to `/changes/new` as the primary CTA (since a change request ID failing likely means it doesn't exist). Keep "Naar home" as secondary.

    All error.tsx files are `"use client"` components per Next.js requirement for error boundaries. Import from `"next/link"` for the home link. Each logs `console.error(error)` for debugging (per UI-SPEC).

    Add `role="alert"` to the outer div for screen reader announcement.
  </action>
  <verify>
    <automated>
      for f in app/error.tsx app/benchmarks/error.tsx app/benchmark-aanvraag/error.tsx app/changes/new/error.tsx "app/changes/[id]/error.tsx" app/admin/client-config/error.tsx app/updates/error.tsx; do
        if [ ! -f "$f" ]; then echo "MISSING: $f"; exit 1; fi
      done
      echo "All error.tsx files exist"
    </automated>
  </verify>
  <done>
    Seven error.tsx files created matching every route group. Each has unique Dutch error copy per UI-SPEC. Root catches all. Dynamic route provides navigation fallback instead of retry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error.tsx → user | Error messages are static Dutch copy — no user input or dynamic data in error display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Information Disclosure | error.tsx console.error | low | accept | Logs to client console only; no PII/server secrets in Error objects (server errors won't propagate error details to client Error objects in production) |
</threat_model>

<verification>
- [ ] `npm run build` succeeds after deploying Plan 01 + Plan 02 (build catches any import issues)
- [ ] Each loading.tsx renders skeleton matching its page layout shape
- [ ] Each error.tsx renders with correct copy and retry button
- [ ] Root error.tsx catches errors from any route not covered by a more specific error.tsx
</verification>

<success_criteria>
- All 6 async route groups have loading.tsx with shimmer skeletons
- All 7 route groups (root + 6 route groups) have error.tsx with retry + home navigation
- Copywriting matches UI-SPEC Error States table exactly (Dutch locale)
- error.tsx files use "use client" directive and import from "next/link"
- Dynamic route [id]/error.tsx navigates to /changes/new instead of retrying
</success_criteria>
