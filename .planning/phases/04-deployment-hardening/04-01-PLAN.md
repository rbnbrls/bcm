---
phase: 04-deployment-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - app/api/health/route.ts
  - tests/api/health.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Docker image builds faster on repeated CI runs due to layer caching improvements
    - Docker image is smaller (fewer layers, excluded build artifacts)
    - Health endpoint returns app + DB status without SSR overhead
    - HEALTHCHECK uses reliable curl command against dedicated endpoint
    - Container won't restart-loop if DB is transiently unavailable (health endpoint reports degraded, not crash)
  artifacts:
    - .dockerignore
    - app/api/health/route.ts
    - tests/api/health.test.ts
  key_links:
    - Dockerfile HEALTHCHECK -> /api/health endpoint
    - /api/health -> PostgreSQL connectivity check
    - .dockerignore -> npm ci / next build cache invalidation
---

<objective>
Harden Docker image build and container health checking.

Purpose: Reduce image build time, shrink image size, and ensure reliable health checks that distinguish "app running" from "app truly healthy" without causing restart loops.

Output: Optimized Dockerfile, new /api/health endpoint, and HEALTHCHECK tuned for zero-downtime deployments.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Dockerfile
@docker-compose.yml
@docker-compose.yaml
@scripts/startup.mjs
@app/api/coolify-status/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .dockerignore and optimize Dockerfile layer ordering</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create `.dockerignore` to exclude from Docker context:
    - node_modules/
    - .next/
    - .git/
    - .env* (but not .env.example — not in .dockerignore scope since it's not in .gitignore either; however .env files with secrets must never enter build context)
    - *.md (all markdown docs — not needed at runtime)
    - tsconfig.tsbuildinfo
    - vitest.config.ts
    - playwright.config.ts
    - playwright-report/
    - test-results/
    - public/commits.json
    - tests/
    - .github/

    Optimize Dockerfile:
    1. Move the apt-get install of curl+ca-certificates BEFORE the COPY --from=builder lines so the layer is shared across all builds (rarely changes)
    2. Keep the current dependency stage (package.json before source) — but use `COPY --link` for the package.json layer to avoid invalidating downstream layers when source changes
    3. Verify the existing layer ordering already does `package.json` before source (it does: dependencies stage copies package.json, builder stage copies everything). Add comments documenting the layer strategy.
    4. Combine the ENV lines into a single layer (already close together).
    5. Keep the separate `COPY --from=builder` lines for standalone output, static, public, scripts, package.json, and postgres module — these are fine for cache granularity.
    6. Add a comment header documenting the layer strategy (base → deps → builder → runner layers).
    7. Ensure `rm -rf /var/lib/apt/lists/*` is on the same RUN line as apt-get install (it already is).
  </action>
  <verify>
    <automated>docker build --no-cache -t bcm-test . 2>&1 | tail -5 && docker images bcm-test --format "{{.Size}}"</automated>
  </verify>
  <done>
    - .dockerignore exists excluding all listed patterns
    - Dockerfile builds successfully with `docker build .`
    - Image size is reasonable (&lt; 500MB)
    - Layer structure visible in `docker history bcm-test` shows ordered dependency→builder→runner layers
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create /api/health endpoint with tests</name>
  <files>app/api/health/route.ts, tests/api/health.test.ts</files>
  <behavior>
    - GET /api/health returns 200 + { status: "healthy", timestamp: string, db: "connected"|"disconnected" } when DB is reachable
    - GET /api/health returns 503 + { status: "degraded", timestamp: string, db: "error"|"disconnected" } when DB is unreachable
    - GET /api/health returns 200 + { db: "disconnected" } when no DATABASE_URL set (demo mode)
    - Response includes CORS headers matching existing API patterns
    - Response time under 100ms (no SSR, no heavy imports)
  </behavior>
  <action>
    Create `app/api/health/route.ts`:
    - `export const dynamic = "force-dynamic"`
    - Import `postgres` from "postgres"
    - In the handler:
      - Try a lightweight `SELECT 1` with a 3-second timeout
      - On success: return NextResponse.json({ status: "healthy", timestamp: new Date().toISOString(), db: "connected" })
      - No DATABASE_URL: return 200 with { status: "healthy", db: "disconnected" } (app works in demo mode)
      - On failure: return NextResponse.json({ status: "degraded", timestamp: new Date().toISOString(), db: "error" }, { status: 503 })
    - Wrap in try/catch with console.error logging on failure
    - Do NOT add postgres to the server component bundle (use dynamic import)
    - Keep the endpoint minimal — no external deps beyond postgres

    Create `tests/api/health.test.ts`:
    - Mock postgres to test all three states (connected, no DATABASE_URL, error)
    - Follow the pattern from tests/api/coolify-status.test.ts (vitest with global fetch mocking pattern)

    Update Dockerfile HEALTHCHECK:
    - Replace the `node -e "require('http')..."` HEALTHCHECK with curl:
      `HEALTHCHECK --start-period=30s --interval=30s --timeout=10s --retries=3 CMD curl -f http://localhost:3000/api/health || exit 1`
    - Keep start-period at 30s (reduced from 60s since /api/health is fast and startup.mjs already waits for DB)
    - curl is already installed in the runner stage via apt-get.
    - Add a comment explaining why curl vs node-http (curl exits immediately with status code, no Node.js process overhead).
  </action>
  <verify>
    <automated>npm test -- tests/api/health.test.ts 2>&1</automated>
  </verify>
  <done>
    - /api/health returns 200 with db status when DB is available
    - /api/health returns 200 with db:"disconnected" when no DATABASE_URL (demo mode)
    - /api/health returns 503 when DB is unreachable
    - HEALTHCHECK uses curl against /api/health with 30s start period
    - Tests pass for all three scenarios
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container health endpoint (internal) | /api/health is only reachable inside the container network (not exposed via Traefik) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | /api/health | low | accept | Endpoint reveals DB connectivity status but only inside Docker network (not publicly routed). Accept minimal disclosure for operational value. |
| T-04-02 | Tampering | Docker build context | low | mitigate | .dockerignore prevents .env files and node_modules from entering build context. Builder never has access to production secrets. |
| T-04-SC | Tampering | npm install in build | high | mitigate | Packages are from npm registry only; package-lock.json pins transitive deps. Docker layer cache prevents mid-build injection. |
</threat_model>

<verification>
- `docker build .` succeeds
- `npm test` passes (includes new health endpoint tests)
- HEALTHCHECK curl command is syntactically valid (no shell errors)
</verification>

<success_criteria>
- Docker image builds in CI & local
- Image layers cache effectively (package.json changes don't invalidate source layers, source changes don't invalidate npm ci layer)
- /api/health returns correct status for all DB states
- HEALTHCHECK uses curl to /api/health with reduced start-period
</success_criteria>

<output>
Create `.planning/phases/04-deployment-hardening/04-01-SUMMARY.md` when done
</output>
